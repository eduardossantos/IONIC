import { AmareloDirective } from './amarelo.directive';

describe('AmareloDirective', () => {
  it('should create an instance', () => {
    const directive = new AmareloDirective();
    expect(directive).toBeTruthy();
  });
});
